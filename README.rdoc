To Doodles is a basic to-do application build using Ruby on Rails.

With To Doodles you can create lists, add tasks to specific lists, delete tasks and delete lists.

There is also an API that can be interacted with from the toodles.rb file that provides similar functionality from the command line.